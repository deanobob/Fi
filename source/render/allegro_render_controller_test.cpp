/// @file math_test.cpp

#include "catch2/catch.hpp"
#include "allegro_render_controller.hpp"

TEST_CASE("render/allegro_render_controller.hpp - renderer tests", "[render]")
{
    SECTION("1 Create Window")
    {
    //    render::allegro_render_controller renderer{nullptr};
    //    REQUIRE(renderer.initialise());
    //    REQUIRE(renderer.create_window(render::window_properties()));
    //    renderer.flip();
    //    sleep(5);
    //    renderer.destroy_window();
    }
}
