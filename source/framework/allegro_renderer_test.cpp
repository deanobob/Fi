/// @file math_test.cpp

#include "catch2/catch.hpp"
#include "allegro_renderer.hpp"

TEST_CASE("framework/allegro_renderer.hpp - renderer tests", "[utilities]")
{
    SECTION("1 Create Window")
    {
    //    framework::allegro_renderer renderer{nullptr};
    //    REQUIRE(renderer.initialise());
    //    REQUIRE(renderer.create_window(framework::window_properties()));
    //    renderer.flip();
    //    sleep(5);
    //    renderer.destroy_window();
    }
}
